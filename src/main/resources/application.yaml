spring:
  application:
    name: unishare

  datasource:
    url: jdbc:mysql://localhost:3306/unishare_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: "0000"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: e4308aa54a888a
    password: "76b3d50a3705b2"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

jwt:
  secret: "uN3$kP9@mX2#vQ7!rL5&wJ8^yT4*hD6ZbF1nA0sCeGiKoMpRuVxYz"
  expiration: 86400000

file:
  upload-dir: uploads

logging:
  level:
    org.springframework.security: DEBUG


